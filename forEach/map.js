//for each map

// let newMap = new Map();

// newMap.set("a", "animal");
// newMap.set("b", "ball");
// newMap.set("c", "choclate");
// // console.log(newMap);

// newMap.forEach((values, key, map) => {
//   console.log(values, key, map);
// });

let newSet = new Set(["animal", "port", "shipping", "telcom"]);
newSet.forEach((n, key, set) => {
  console.log(n, key, set);
});
